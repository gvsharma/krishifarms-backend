name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    # Nightly full E2E suite (02:30 UTC)
    - cron: "30 2 * * *"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/validate.yml

  wake-ec2:
    name: Wake shared EC2
    uses: ./.github/workflows/wake-ec2.yml
    secrets: inherit

  e2e:
    name: E2E (Playwright)
    needs: [validate, wake-ec2]
    uses: ./.github/workflows/e2e.yml
    secrets: inherit
