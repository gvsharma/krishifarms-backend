# Vercel / remote API template. For local dev, use .env.local.example when the app exists.
#
# Pair with backend on EC2 (see deploy/env/application.env.example).

# --- Vercel + dev EC2 (set on Vercel Production) ---
NEXT_PUBLIC_API_BASE_URL=/api/v1
API_PROXY_TARGET=http://13.232.200.243
NEXT_PUBLIC_SITE_URL=https://krishifarms.vercel.app

# Backend CORS_ORIGINS must include NEXT_PUBLIC_SITE_URL.
