"use client";

import { Sprout } from "lucide-react";
import { PlaceholderPage } from "@/components/shell/placeholder-page";

export default function FarmsPage() {
  return (
    <PlaceholderPage
      titleKey="pages.farms"
      descriptionKey="pages.farmsDesc"
      icon={Sprout}
    />
  );
}
